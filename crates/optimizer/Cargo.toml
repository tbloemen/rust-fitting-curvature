[package]
name = "fitting-optimizer"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "optimizer"
path = "src/main.rs"

[dependencies]
fitting-core = { path = "../core" }
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
